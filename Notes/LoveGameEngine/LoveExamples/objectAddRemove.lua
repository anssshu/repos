[[ a = {}
    for line in io.lines() do
      table.insert(a, line)
    end
    print(table.getn(a))
    
table.remove(a,line)


draw each object in the table
inside the draw function

do the removal and adition of objects inside the update function

]]